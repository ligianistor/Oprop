package testcases.cager.jexpr;

import junit.framework.AssertionFailedError;
import junit.framework.Assert;
import junit.framework.Test;
import junit.framework.TestCase;
import junit.framework.TestSuite;

public class TestJExpr extends TestCase {

    public TestJExpr(String name) {
        super(name);
    }

    public void setUp() {
    }

    public void testJExpr()
    {/*
        Assert.assertEquals(cager.jexpr.test.Temp.stringFunc("YY"), "YY");
        Assert.assertEquals(cager.jexpr.test.Temp.testInt2(null, null), 3);
        Assert.assertEquals(cager.jexpr.test.Temp.stringFunc2("XX"), "XXHello");
        Assert.assertNull(cager.jexpr.test.Temp.nullVector());
        Assert.assertTrue(cager.jexpr.test.Temp.someVector(new java.util.Vector()) instanceof java.util.Vector);

        Object obj1 = new java.util.ArrayList();
        Object obj2 = new Object();

        Assert.assertEquals(cager.jexpr.test.Temp.stringConcat1(obj1), obj1 + ">");
        Assert.assertEquals(cager.jexpr.test.Temp.stringConcat2(obj1, obj2), obj1 + " : " + obj2);

        Assert.assertSame(cager.jexpr.test.Temp.stringCast("XX"), "XX");       // Use == rather than .equals to compare refs.

        Assert.assertEquals(cager.jexpr.test.Temp.testIf(1,2), 2);
        Assert.assertEquals(cager.jexpr.test.Temp.testIf(2,1), 2);

        Assert.assertEquals(cager.jexpr.test.Temp.testChar('A', 1), 'B');

        Assert.assertEquals(cager.jexpr.test.Temp.implicitWiden((short)12), 12);

        // Uncomment to make it fail!
        //Assert.assertEquals(cager.jexpr.test.Temp.implicitWiden((short)12), 18);
*/
        System.out.println("Completed test of generated class");
    }
}